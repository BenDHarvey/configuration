---
all:
  hosts:
    control-docker-host-1:
    control-plex-1:
    control-backups-1:
    control-rancher-1:

  children:
    control-docker-hosts:
      hosts:
        control-docker-host-1:

    control-plex:
      hosts:
        control-plex-1:

    backups:
      hosts:
        control-backups-1:

    control-rancher:
      hosts:
        control-rancher-1:
